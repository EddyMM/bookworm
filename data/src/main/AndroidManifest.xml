<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.eddy.data">

    <application>
        <service android:name=".syncservices.SyncCategoriesIntentService"/>
        <service android:name=".syncservices.SyncBooksIntentService"/>

        <activity android:name=".settings.SettingsActivity"/>
    </application>
</manifest>
