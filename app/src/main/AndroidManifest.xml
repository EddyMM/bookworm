<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.eddy.bookworm">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".base.BookwormApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_bookworm_icon_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".listnames.ListNamesActivity"
            android:launchMode="singleTop">
        </activity>
        <activity
            android:name=".bookslist.BooksListActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".listnames.ListNamesActivity"
            tools:targetApi="jelly_bean">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".listnames.ListNamesActivity"/>
        </activity>

        <activity
            android:name=".bookslist.BookmarksListActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".listnames.ListNamesActivity"
            tools:targetApi="jelly_bean">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".listnames.ListNamesActivity"/>
        </activity>

        <activity
            android:name=".bookdetail.BookDetailActivity"
            android:label="@string/title_activity_book_detail"
            android:parentActivityName=".bookslist.BooksListActivity"
            android:theme="@style/AppTheme.NoActionBar"
            tools:targetApi="jelly_bean">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.eddy.bookworm.bookslist.BooksListActivity"/>
        </activity>
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver android:name=".widget.BookwormRandomBestSellerWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bookworm_random_best_seller_widget_info"/>
        </receiver>

        <activity android:name=".widget.BookwormRandomBestSellerWidgetConfigureActivity">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE"/>
            </intent-filter>
        </activity>
        <service android:name=".widget.GetRandomBookIntentService"/>
    </application>

</manifest>